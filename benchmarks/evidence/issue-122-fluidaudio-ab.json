{
  "_what_this_is": "Measurements taken for #122 (FluidAudio 0.15.4 -> 0.15.5), together with their scope, provenance and reproduction caveats. Higher-level conclusions are in the CHANGELOG entry, which cites this file.",
  "_reproducing": "Recomputable from what is recorded here: the corpus hashes; both arms' transcript hashes; the nm counts; `corpus_duration_seconds` (the corpora registry); `fluidaudio_revision` (Package.resolved); and `metric`. For `metric`, both sides have to be the right artifact: the hypothesis is the **txt** transcript (raw), and the reference is an SRT, so it goes through `SRTParser.parse` then `SRTParser.referenceText(from:)` first. `TextNormalizer` is not a separate step — `ErrorRate.wer` calls it internally. Three ways to get this wrong, all measured: raw .srt text as the reference gives edits=6 / ref_words=83 / wer=0.07228915662650602; the raw srt transcript as the hypothesis against a parsed reference gives 0.075; both raw gives 0.060240963855421686. Only the combination above gives the recorded 0.0375. Pre-normalising both sides first was also measured and does **not** change the result (0.0375), because `TextNormalizer.normalize` is idempotent; it is listed here as a checked non-issue, not as a fourth way to get it wrong. Not recomputable: `executable_sha256` (see its note); `measured_at` / `machine` / `toolchain`, which are observations of one run; and all of `path_coverage`, which needs the instrumentation `path_coverage.how` describes. Provenance, one unit for the whole block: **every field in `path_coverage` has been reproduced by at least two separately written instrumentations**, by verifiers across rounds 3 to 8 who did not see each other's probes. Two limits on what that is worth. The runs were **not blind** — each verifier was given the recorded values to check against — so they establish reproducibility, not independent discovery, and they do not exclude a verifier stopping once the expected number appeared. And they were all dispatched by the author on one machine; they are separate instrumentations, not separate laboratories. The verification comments on PR #142 link the runs; that is a pointer, not a substitute for redoing it.",
  "session": {
    "measured_at": "2026-08-05",
    "machine": "Apple M5 Max, macOS 27",
    "toolchain": "Apple Swift 6.2.4 (swift-6.2.4-RELEASE), the PATH default on this machine",
    "toolchain_caveat": "`swift build -c release` crashes under this toolchain — SIL memory lifetime failure in OwnershipModelEliminator compiling swift-transformers' Tokenizers, unrelated to FluidAudio. scripts/install.sh documents the same crash as \"SIL specialization\"; this description is the more precise of the two. These are debug builds, which is why the nm counts under `arms` are debug counts.",
    "corpus": "osr-harvard-1",
    "corpus_duration_seconds": 33.623125,
    "language": "en",
    "audio_sha256": "0ed4ea79ee09b36f40235992b5bc03009f23167c0525930ddeafee0c04716a49",
    "reference_sha256": "644c935af618dde484af146260166bb8c8d9be244ff467b1964b99d48d81c35e",
    "transcribe_command": "<realpath .build/arm64-apple-macosx/debug/bestasr> transcribe <audio> --backend fluid-parakeet --model 0.6b-v3 --language en --hallucination-filter off --format srt --output <out.srt>   # and again with `--format txt --output <out.txt>`; two invocations, one per format",
    "nm_command": "nm <exe> | grep -c caseVariantCanonicalIds",
    "notes": [
      "`transcribe`, not `benchmark`: benchmark writes to ~/.bestasr/store and has no --store-dir (issue #160), so it cannot run without touching durable user data. transcribe reads the store for routing and writes nothing.",
      "Invoked by realpath: `bestasr` is not resolvable on PATH on this machine, so a bare `bestasr …` command string would not run at all.",
      "--hallucination-filter off removes post-processing. Both formats because a moved seam shows up in SRT timecodes before it shows up in words.",
      "Two runs per pin. This bounds decoder variability rather than excluding it: no run-to-run difference was observed in four runs."
    ]
  },
  "arms": {
    "0.15.4": {
      "fluidaudio_revision": "b9d43724cbdb5a980e441fd54180964e94d470f7",
      "executable_sha256": "000cc31f7631b12861f06f12bf0d62124f1d631df8b72c9056460438c204dc04",
      "nm_caseVariantCanonicalIds": 0,
      "transcript_sha256_srt_run1": "02af260ccc3dc09cac0cdc5e10af9ea2a5f39373a6577310653f532b9b072d93",
      "transcript_sha256_srt_run2": "02af260ccc3dc09cac0cdc5e10af9ea2a5f39373a6577310653f532b9b072d93",
      "transcript_sha256_txt": "d69786c7bcb0dec02d751e55699a60597e9d4c84b932070f306795507d950589"
    },
    "0.15.5": {
      "fluidaudio_revision": "19600a485baa4998812e4654b70d2bab8f2c9949",
      "executable_sha256": "e4831451d9e85d7e818e8f00c1284e91ac26f9b81d8a08f708cc124e4bd16e71",
      "nm_caseVariantCanonicalIds": 4,
      "transcript_sha256_srt_run1": "02af260ccc3dc09cac0cdc5e10af9ea2a5f39373a6577310653f532b9b072d93",
      "transcript_sha256_srt_run2": "02af260ccc3dc09cac0cdc5e10af9ea2a5f39373a6577310653f532b9b072d93",
      "transcript_sha256_txt": "d69786c7bcb0dec02d751e55699a60597e9d4c84b932070f306795507d950589"
    },
    "cross_pin_cmp": "srt identical, txt identical (byte comparison, not equal-WER)",
    "_executable_hash_note": "Recorded to identify the build these numbers came from, NOT as a reproducible value — the same pin built twice yields different hashes. Two purposes, two fields: the revision says which dependency source was expected, the hash says which artifact was measured.",
    "_nm_note": "This count varies across the four cells below, which differ in build configuration AND toolchain together — the two cannot be separated from these measurements, so no claim is made that configuration alone is responsible. Cells: debug/plain 4, debug/-a 8 (toolchain in `session`); release/plain 2, release/-a 4 (Apple Swift 6.3.3, swiftlang-6.3.3.1.3, at /usr/bin/swift; Xcode 26.6 build 17F113; xcode-select -p = /Applications/Xcode.app/Contents/Developer; arm64). A different toolchain was needed for the release cells because the one in `session` did not build this target in release under the measured setup. Across those four cells 0.15.4 was 0 and 0.15.5 was non-zero. No claim is made for other toolchains, SDKs, linkers, build settings, stripping, architectures or nm options. An earlier version recorded 4 without saying which build or flag produced it; a third party running plain nm on a release build got 2."
  },
  "metric": {
    "kind": "wer",
    "edits": 3,
    "reference_words": 80,
    "value": 0.0375,
    "edit_list": [
      "it's -> it is (substitution + insertion)",
      "parked -> park (substitution)"
    ],
    "_note": "Numerator and denominator recorded so the value can be re-derived. Identical on both arms, which follows from the transcripts being byte-identical."
  },
  "path_coverage": {
    "how": {
      "patch": "stderr probes compiled into `.build/checkouts/FluidAudio/…/TDT/ChunkProcessor.swift`. Patch the 0.15.5 checkout (`19600a48…`); `caseVariantIds` does not exist in 0.15.4, so the patch will not apply to that arm. The file is mode 444: `chmod u+w` before editing, `chmod 444` after. **Restoring the mode is not restoring the file.** Revert the edit as well — `git -C .build/checkouts/FluidAudio checkout -- <path>` — or every later build silently keeps the probes. `.build/checkouts/` is outside this repo's version control, so `git status` will never mention it. That is the mirror of the failure in `limits[0]`: there, a build that never took the patch looks clean; here, a build that was never un-patched looks clean.",
      "build": "Debug. The toolchain is in `session`; `session.toolchain_caveat` says why release was not used. Nothing here establishes that these counts are independent of build configuration — they were not measured under any other.",
      "probe_implementation": "One line per event to stderr, counted afterwards with `grep -o … | wc -l` rather than `grep -c`, which counts occurrences rather than lines in case two markers ever share one. **Every probe site below is on the sequential path**: `withThrowingTaskGroup` closes before `orderedChunkOutputs` is formed, and the merge loop, `collapseSeamWordDuplicates` and everything they call run in a plain `for` loop after it, so a global counter printed at exit works equally well and the two counting methods agree. Chunk decoding above that point *is* concurrent — pool size `max(1, manager.parallelChunkConcurrency)`, not a fixed number — so a counter placed there would need synchronising; none of the fields below are counted there. Probing the fold match and the differing-canonicals case separately requires expanding `return lhs == rhs` into an if/else, so the instrumentation is not purely additive at that site (see `_limits[4]`).",
      "probes": {
        "chunks": "merge loop: the count of ordered chunk outputs.",
        "merges": "merge loop: one emit per merge.",
        "case_folded_calls": "probe on the first statement of `tokenIdsMatch`.",
        "case_folded_early_return_ids_equal": "probe inside its `left == right` branch.",
        "case_folded_reached_guard": "obtainable either way, and both were done: probed directly at the guard, and derived as `case_folded_calls - case_folded_early_return_ids_equal`, which also equals `guard_else + matches + canonicals_differed`. All three agree at 773. The subtraction is valid only because `tokenIdsMatch` has no exit between the early return and the guard — read the function before relying on it rather than on a probe.",
        "case_folded_guard_else": "probe in the guard's `else`. Decomposed by the next two fields.",
        "case_folded_guard_else_map_nil": "probe splitting the `else` on `caseVariantIds == nil`. The guard binds the map first, so a nil map sends every unequal-id call here; on the shipped path it is never nil.",
        "case_folded_guard_else_id_absent": "the `else`'s other branch: the map is present and at least one of the two ids is absent from it.",
        "case_folded_both_ids_in_map": "obtainable either way, and both were done: probed on the path past the guard, and derived as `case_folded_matches + case_folded_canonicals_differed`. Both agree at 133.",
        "case_folded_canonicals_differed": "probe on `lhs != rhs`.",
        "case_folded_matches": "probe on `lhs == rhs`.",
        "case_folded_matched_token_ids": "emitted at the fold match.",
        "case_folded_canonical_id": "emitted at the fold match.",
        "collapse_called": "probe at the entry of `collapseSeamWordDuplicates`.",
        "collapse_tokens_in": "token count at that entry.",
        "collapse_tokens_out": "token count at each of its exits.",
        "word_boundary_fallbacks_entered": "the **sum** of three separately probed sites, each 0: `mergeUsingMatches`' tail re-splice (probed at the block entry, the widest reading), `popSeamWord`'s safe-id branch, and `mergeByMidpoint`'s right-scan. One field, three sites — it is their sum, not their max nor the number of sites entered. All three readings coincide at 0, so this distinction is currently unfalsifiable.",
        "counterfactual_tokens_equal": "per-merge comparison against the shipped output; see `isolating_the_counterfactual`.",
        "counterfactual_timestamps_equal": "per-merge comparison against the shipped output.",
        "case_folded_changed_merge_output": "follows from the two `counterfactual_*` comparisons."
      },
      "isolating_the_counterfactual": "The counterfactual recomputes each merge with `caseVariantIds: nil`, starting from the **shipped path's** left input — not from a parallel nil chain fed by its own previous output. It runs alongside the shipped path in the same process, which is what makes `counterfactual_tokens_equal` computable at all, and it therefore **shares the counters. Building it separately does not change that.** The only thing that isolates them is tagging: thread a tag through `mergeChunks → tokensMatch → tokenIdsMatch` and count `shipped` and `cf` separately.\nUntagged, `case_folded_calls` / `case_folded_early_return_ids_equal` / `case_folded_reached_guard` / `case_folded_guard_else` come out at 1688 / 142 / 1546 / 1413 — the last is not a doubling because with a nil map every call reaching the guard takes the `else`, so the counterfactual contributes 773 there rather than 640.\nAll counts here are **per invocation** of `session.transcribe_command`; the srt and txt invocations give identical counts. Summing the two invocations instead gives 1688 / 142 / 1546 / **1280**. Three of those four coincide with the untagged figures above, so `case_folded_guard_else` is the only field that tells the two mistakes apart: 1413 means an untagged counterfactual, 1280 means two invocations added together.",
      "marker_check": "Before believing any count, check every one of your own probe markers is in the binary, and make the check stop the run rather than merely report:\n    : \"${exe:?}\"\n    (( ${#markers[@]} )) || { echo \"no markers to check\" >&2; exit 1; }\n    for m in \"${markers[@]}\"; do\n      : \"${m:?empty marker}\"\n      strings \"$exe\" | grep -Fq -- \"$m\" || { echo \"missing probe marker: $m\" >&2; exit 1; }\n    done\nThe array-length line is not decoration: without it, an empty or unset `markers` runs the loop zero times and exits 0 — a check that verified nothing, reporting success — and bash and zsh disagree about which of those two cases does what. The empty-element guard is a separate failure: an empty marker matches almost any output. Verified in both shells: a present marker passes; a missing marker, an empty element, and an empty or unset array each exit 1.",
      "limits": [
        "A build that did not pick up the patch produces a probe-free binary whose output is trivially identical to the clean one, which reads as success. `marker_check` is what catches this; nothing else recorded here does.",
        "`marker_check` is necessary, not sufficient: it shows the literals are in the binary, not that every probe site is, nor that any probe ran.",
        "`strings` emits only printable runs of 4 characters or more by default, so a short marker sitting in an isolated run is reported missing when it is in fact present. Use markers well above that length, or pass `strings -n`."
      ]
    },
    "chunks": 3,
    "merges": 2,
    "case_folded_matches": 2,
    "case_folded_calls": 844,
    "case_folded_early_return_ids_equal": 71,
    "case_folded_reached_guard": 773,
    "case_folded_guard_else": 640,
    "case_folded_guard_else_map_nil": 0,
    "case_folded_guard_else_id_absent": 640,
    "case_folded_both_ids_in_map": 133,
    "case_folded_canonicals_differed": 131,
    "case_folded_matched_token_ids": [
      375,
      518
    ],
    "case_folded_canonical_id": 375,
    "case_folded_changed_merge_output": false,
    "collapse_called": true,
    "collapse_tokens_in": 154,
    "collapse_tokens_out": 154,
    "word_boundary_fallbacks_entered": 0,
    "counterfactual_tokens_equal": true,
    "counterfactual_timestamps_equal": true,
    "_limits": [
      "375 and 518 are ' r' and ' R' in the vocabulary; 0.15.4's matcher returns false for that pair because it compares ids for equality.",
      "\"Entered\" has two natural cut points, because the guard binds three conditions at once: reaching it (773) and passing it with both ids in the map (133). Say which one a count refers to. `case_folded_matches` is neither — it is the 2 times the branch returned a match. The guard's `else` has three possible causes and is now split into two recorded fields: map nil (0) and an id absent from the map (640). An earlier version of this file said \"Only the match count has been reproduced identically by every independent run\" and used that to withhold these numbers. It was false: the runs were consistent, and had taken different cut points.",
      "collapse_tokens_in == collapse_tokens_out establishes that no token was removed. It does not by itself establish that ids, order or timestamps are unchanged.",
      "The counterfactual isolates the incremental effect of supplying caseVariantIds inside a 0.15.5 binary. It is not a 0.15.4 execution, and it assumes the nil path is equivalent to 0.15.4's matcher.",
      "The probe build's transcript hash equals the clean build's, so no final-output perturbation was observed. That does not establish that the instrumentation was control-flow-neutral."
    ]
  },
  "store_note": "An earlier 0.15.4 run wrote a measurements.jsonl row stamped app_version 0.16.0 — the version shipping with 0.15.5 — because the schema has no FluidAudio field, so the row could not say what condition produced it. That row was removed rather than left to be ranked against honest ones; a backup was kept at ~/.bestasr/store/measurements.jsonl.bak-idd122-20260803161704, which is machine-local and therefore not auditable from this repo. The measurements recorded here wrote nothing to the store — verified by hashing all five store files before and after."
}
