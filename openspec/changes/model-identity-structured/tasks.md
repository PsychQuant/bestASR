## 1. 型別

- [x] 1.1 [P] 新增 `Sources/BestASRKit/Models/ModelID.swift`，定義 `ModelID{family, size}`（`Hashable` / `Codable` / `Sendable`）。**行為**：兩個 family 與 size 皆相同的值相等且雜湊相同；family 或 size 為空字串時建構失敗。**驗證**：新增測試檔 `Tests/BestASRKitTests/ModelIDTests.swift`（本任務建立；Swift Testing），內含相等性、雜湊、空值拒絕三則測試。（實作 design D1：`ModelID` 只含 `family` + `size`，不含 quantization 與 runtime。滿足 model-grid 的 "Model identity is a structured value, not a formatted string"）
- [x] 1.2 [P] 於同檔新增 `Quantization` 封閉列舉，四個 case：`notApplicable` / `named(String)` / `deferred(Deferrer)`（`Deferrer` 為 `.runtime` 或 `.dependency`）/ `unknown`。**行為**：`named` 的 `Codable` 序列化直出其字串值；其餘三者各有保留字且互不混淆。**驗證**：於任務 1.1 建立的 `Tests/BestASRKitTests/ModelIDTests.swift` 新增四個 case 的 encode/decode round-trip 測試，並主張 `deferred` 與 `unknown` 解碼後不相等。（實作 design D4：quantization 成為封閉列舉，四個 case。滿足 model-grid 的 "Quantization is a closed enumeration with no placeholder"）

## 2. 目錄與序列化

- [x] 2.1 `ModelRow`（於 `Sources/BestASRKit/Store/StoreTables.swift`）改為持有 `ModelID` 與 `Quantization`，取代四個鬆散字串欄位；`ModelRow.id(...)` 保留為相容入口，內部由 `ModelID` 組出四段字串。**行為**：序列化形式維持 `runtime|family|size|quantization`；`ModelRow` 的 JSON 欄位維持平坦（`family` / `size` / `quantization` 三個頂層字串鍵），不因型別改變而變成巢狀。**驗證**：新增測試檔 `Tests/BestASRKitTests/ModelRowCodecTests.swift`（本任務建立；Swift Testing），讀入**已提交的** `docs/model-identity-audit.csv`（37 筆，內容為 `~/.bestasr/store/models.jsonl` 的快照）的 `model_id` 字串、重新序列化，主張與輸入**逐字相同**。以已提交檔為準而非家目錄檔，是因為後者在 CI 不存在時測試會靜默空過；測試須主張檔案存在、列數非零，且其中**至少各有一列** size 與 quantization 為 `default`，以確保最難的兩類確實被走到。`~/.bestasr/store/models.jsonl` 存在時另做一次相同主張作為交叉檢查，不存在則跳過該則而不影響前者。（實作 design D2：對機序列化沿用四段字串，由 `ModelID` 產出。滿足 benchmark-store 的 "The persisted model key is unchanged by the identity refactor"）
- [x] 2.2 移除 `ModelGrid.row(backend:modelAddress:)` 的雙文法與裸 size fallback，改為以 `ModelID` + runtime 查找。**行為**：任一查找至多命中**一個模型**；不再有「first row wins」的靜默歧義。（同一模型底下仍可有多列——那是量化變體，依偏好排序，如 `whisper.cpp tiny` 的 `q5_1` 與 `q8_0`；被消除的是**跨模型**的歧義。）使用者輸入的字串另由 `ModelGrid.identity(backend:matching:)` 解析：命中零個或多於一個模型時回 nil，由呼叫端據此拒絕，而非代為挑一個。**驗證**：`ModelGridTests` 新增測試，主張 `ModelID("canary","1b")` 與 `ModelID("mms","1b")` 各自命中自己的列且互不返回對方；並主張裸字串 `"1b"` 解析為 nil（歧義），而 `whisper.cpp` 的 `"tiny"` 仍解析成功（無歧義的裸 size 不受影響）。（滿足 model-grid 的 "Model identity is a structured value, not a formatted string"）
- [x] 2.3 **（讀時 canonical 化解鎖，已落地）** 正規化 grid 列的 `size`，使其等於 pinned upstream artifact 宣告的版本。已知待改：`mlx-audio` 的 parakeet 列 size 由 `0.6b` 改為 `0.6b-v3`（pin 為 `mlx-community/parakeet-tdt-0.6b-v3`）。**行為**：同一模型在不同 runtime 下取得相同 `ModelID`。**驗證**：`ModelGridTests` 新增一則測試，主張 fluid-parakeet 與 mlx-audio 兩列的 `ModelID` **相等**。（實作 design D3：`size` 正規化是前提，不是附帶清理。滿足 model-grid 的 "Size is normalised to the pinned upstream artifact"）
- [x] 2.4 **（讀時 canonical 化解鎖，已落地）** 將 grid 每一列的 quantization 由字串改為 `Quantization` 值，依 `docs/model-identity-audit.csv` 的分類逐列指派，**19 列全部涵蓋**：`apple-speech` 1 列 → `notApplicable`；`fluid-*` 3 列 → `named`（值由任務 5.1 決定：parakeet `int8`、paraformer `fp16`、sensevoice `fp16`）；whisperkit 6 列 → `deferred(.runtime)`；canary → `named("q8")`，取自其 pin `Mediform/canary-1b-v2-mlx-q8`；**其餘 8 列 mlx-audio reference row → `unknown`**——原任務只點名其中 2 列（`mega-asr`、`qwen3-forcedaligner`），漏掉 6 列：4 列 pin 了 repo 但該 repo 未宣告精度（`moonshine/base`、`nemotron-asr/streaming`、`parakeet/0.6b-v3`、`whisper/large-v3-turbo`），2 列連 repo 都沒有（`distil-whisper/large-v3`、`mms/1b`）。這 8 列全屬未 bundle 的 reference catalog，不會進入 benchmark 候選。**行為**：`Sources/` 中不再有任何列以 `default` 作為 quantization 或 size 的值；唯一允許出現該字面值的位置是 `ModelID.removedPlaceholder` 常數本身（見 design 的 Acceptance criteria，該例外為封閉列舉）。**驗證**：`ModelGridTests` 新增三則測試——(a) 沒有任何列的 quantization 是 `.named("default")`；(b) `unknown` 的列**恰好**是上述 8 列（封閉列舉，**不得依性質相似擴充**，新列要成為 `unknown` 必須刻意改這則測試）；(c) apple-speech 為 `notApplicable`、whisperkit 6 列為 `deferred(.runtime)`、fluid 3 列為 `named` 且值分別為 int8 / fp16 / fp16。
  - **未closed的項目（size 軸）**：`mega-asr` 與 `qwen3-forcedaligner` 兩列的 **`size` 仍是 `default`**。spec model-grid 規定該字面值不得作為 size 出現，但上游兩者都沒有公布版本名，而 `ModelID` 要求 size 非空。三個可能解各有代價：(i) 移除這兩列——會讓 `mlxFamilies` 從 15 掉到 13，破壞既有的 reference catalog 主張；(ii) 另編一個 size 字串——那只是換名字的佔位字；(iii) 放寬 spec，容許「family-only」的 reference row。**未擅自選擇**。原本具名這兩列的 `ModelGridTests` 封閉列舉已隨 re-key 移至 #187；現在讓缺口可見的是 `The catalog still spells the placeholder — deferred, not forgotten`（主張 19 列），但 round 5 判定它的逐列迴圈是恆真式、只有 count 有辨識力——**這個守衛比它看起來弱**，強化一併記在 #187。（滿足 model-grid 的 "Quantization is a closed enumeration with no placeholder"）

## 3. 記憶體估計與 router 排序鍵

- [x] 3.1 `ModelRegistry.memoryEstimates` 改以 `ModelID` 為鍵。**`uniquingKeysWith: max` 保留**——原任務要求移除，round 4 證實那是錯的：它吸收的兩種碰撞裡，keying by identity 只解決一種（跨 family 同名），另一種（同一身分兩個精度）是 `ModelGrid` 自己文件稱為正常的情形，`whisper.cpp` 今天就有兩列。移除它會在 `ColdStartPrior.fits()` 的迴圈裡 fatalError。**行為**：`requirements(for: ModelID("sensevoice","small"))` 回 1.5 GB，不再被 `whisper small` 的 2.5 GB 覆蓋。**驗證**：測試加入 `Tests/BestASRKitTests/DataModelTests.swift` 內**既有的** `struct ModelRegistryTests`——原任務寫「新增測試檔 `ModelRegistryTests.swift`（目前不存在）」，該**檔名**確實不存在，但同名 struct 已存在於 `DataModelTests.swift`，另開新檔會重複宣告。內含一則測試主張該值為 1.5 且不等於 `ModelID("whisper","small")` 的 2.5。（滿足 asr-engine 的 "Memory estimates are keyed by the full model identity"）
- [x] 3.2 `accuracyRank` / `nextSmaller` / `profileModels` 改以 `ModelID` 為鍵，並為每個 family 定義各自的順序與降級鏈。**行為**：非 whisper family 不再回傳 -1。accuracy ladder 由 grid 列的 `estMemoryGB` 依 family 分組排序導出（whisper 導出的順序與既有 `supportedModels` 相同，已由測試主張）；**降級鏈另行宣告、不與 ladder 共用**——whisper 的鏈刻意不含 `large-v3-turbo`（它是 `large-v3` 的同儕而非其下一階，兩者都降到 `medium`，#29），由記憶體導出會錯誤地把它插進中間。**驗證**：於任務 3.1 使用的 `DataModelTests.swift` 之 `ModelRegistryTests` 新增一則測試，對 `parakeet` / `paraformer` / `sensevoice` 三個 family 各主張其 rank 非負，且 `nextSmaller` 在同 family 內解析或於鏈尾回 nil。（實作 design D6：`accuracyRank` / `nextSmaller` / `profileModels` 改以 `ModelID` 為鍵。滿足 asr-routing 的 "Candidate ranking covers every family, not only the whisper ladder"）
- [x] 3.3 `Router` 對身分不完整的量測**排序並具名**，不排除。**round 9 更正了 round 8 的方向**：先前實作的是「排除 `!identityComplete` 的記錄」，拿本機 store 實測後判準是**反的**——被排掉的 44 筆（四個 mlx-audio key）全部帶完整 commit sha pin（artifact 凍結），而被 `canonical()` 追認為完整、照常進排序的 47 筆 fluid 記錄，量測當時 engine 呼叫 `load()` **不帶 precision**（顯式 pin 是本 change 才加的）。根因：判準問的是**今天的目錄**，而不是那筆量測自己記了什麼。`MeasurementRow.hfRevision` 早在 #16 就為了同一個理由存在（「the catalog table is rewritten wholesale on every seed, so the pin a number was measured against must live on the measurement itself」），而我的述詞完全沒讀它。**行為**：`BenchmarkRecord.artifactAttested` 由投影從記錄自身的事實計算（stored key 有具體 quantization ∨ measurement 帶 hfRevision ∨ runtime 無量化維度），三者是封閉列舉；不能自證的記錄照常排序，recommendation 以 warning 具名被選中者、以 reason 報出候選池中的比數。**實測**：383 筆 → 55 個候選全部排序，27 個標記（whisperkit `deferred:runtime` 與 fluid-* 標記；mlx-audio sha-pinned、whisper.cpp `q8_0`、apple-speech `n/a` 通過）。**驗證**：`ArtifactAttestationTests` 六則，含一則**兩個 corpus** 的 collapse 測試（單 corpus 會在 `guard group.count > 1` 短路，round-4 C3 的空轉正是這樣來的）。（滿足 benchmark-store 的 "Records carry whether they can attest which artifact produced them"）

## 4. store 投影

- [x] 4.1 [P] `StoreProjection` 移除針對 `mlx-audio` 的三元運算子，改為直接以四段建構 `ModelID`，並於投影結果標示身分是否完整（`BenchmarkRecord` 新增 `identity` 與 `identityComplete`）。**位址是否帶 family 改由「裸 size 在該 runtime 下是否有歧義」決定**，不再依 backend 名稱分支。**`parts[1] == parts[2]` 的 legacy 修補保留**——原任務要求一併移除，但實查 store 發現**仍有 4 個這種 id**（`whisperkit|base|base|default`、`whisperkit|tiny|tiny|default`、`whisper.cpp|base|base|q8_0`、`whisper.cpp|tiny|tiny|q8_0`），移除會讓它們的 family 變成 `base`／`tiny`，與同一候選的新量測靜默分家。該修補的退場屬於歷史記錄重新編碼那個 change；此處僅去掉它對 backend 的條件（目錄中沒有任何 family 等於自己的 size）。**行為**：投影後 family 不再被丟棄；不完整的記錄被標記而非靜默。**驗證**：新增一則測試，主張一筆非 mlx 的 `model_id` 投影後其 `ModelID.family` 等於字串中的第二段（今日為丟棄後補回的 `whisper`），並主張一筆 `unknown` quantization 的記錄被標為不完整。（滿足 benchmark-store 的 "Records carry whether their identity is complete"）

## 5. engine

- [x] 5.1 [P] `ChineseFamilyEngine` 呼叫 `ParaformerManager.load()` 與 `SenseVoiceManager.load()` 時顯式傳入 precision，取代委由 FluidAudio 預設。**行為**：FluidAudio 版本變動不再靜默改變載入的權重（engine 顯式傳 fp16 / fp16 / int8），且**目錄標示與之相符**——三列的 `Quantization` 為 `.named("int8")` / `.named("fp16")` / `.named("fp16")`。實作時發現任務原文只點名 `ChineseFamilyEngine`，但三列中的 fluid-parakeet 其精度是在 `Sources/BestASRKit/Engines/ParakeetEngine.swift` 的 `AsrModels.load(...)` 決定的，故該處一併顯式化。**所選值一律等於 FluidAudio 當前的預設**：`ParaformerPrecision.fp16`、`SenseVoiceEncoderPrecision.fp16`、`ParakeetEncoderPrecision.int8`——選任何其他值都會讓 store 內既有量測的對象在無人察覺下改變，pin 的目的是凍結而非升級。**驗證**：engine 半邊由原始碼直接可讀（`ChineseFamilyEngine` 的 `load(precision: .fp16)` 兩處與 `ParakeetEngine` 的 `encoderPrecision: .int8`）。**原本引用的 `ModelGridTests` 三列斷言已隨 re-key 移至 #187，現不存在**——在 catalog 值回到 `default` 之後，那則斷言無法成立。本任務因此**沒有自動化驗證**，這是拆分留下的覆蓋缺口，記在 #187。（實作 design D5：`deferred(.dependency)` 在本 change 內就地消除。滿足 asr-engine 的 "A runtime states the quantization it loads"）

## 6. 對外字串

- [x] 6.1 [P] `CommandCore` 與 `Sources/bestasr/BestASRCommand.swift` 的 `list-models` 輸出改為 `family size (runtime)`；quantization 為 `deferred` 時輸出其 decider。**行為**：`list-models` 的輸出改為 `family size (runtime)`，且**不含 `default`**（實測：0 次）。**此條僅涵蓋目錄輸出，不涵蓋 `recommend`**——`recommend` 的 measured path 直接帶出量測記錄自身的 `quantization` 字串，而 store 內 383 筆量測有 **344 筆（89%）**的值就是 `default`（先前此處寫「35 筆」是錯的——那只數了兩個 parakeet key，漏掉 whisperkit 的 `large-v3-turbo` 140 筆、`tiny` 41 筆等；round-4 verify 更正）；實跑 `bestasr recommend` 確認其 JSON 的 `quantization` 與 reason 行仍含該字。那是**記錄的事實**、不是目錄的佔位字，在歷史量測重新編碼那個 change 落地前無法消除。**驗證**：`CLITests` 的 `list-models names the model first and the provider second` 主張每列以 family 起首並含 runtime。（原本一併引用的 `list-models never prints the removed placeholder` **已隨 re-key 移至 #187**，現不存在——round 5 指出引用不存在的測試是本 change 反覆出現的缺陷類。）`StoreProjectionIdentityTests` 主張一筆 legacy `default` 記錄的值**逐字保留**（migration 靠它定位）但 `identityComplete` 為 **false**（不背書其可比較性）。（滿足 cli 的 "list-backends and list-models"）
- [x] 6.2 [P] `Sources/BestASRMCPCore/Server.swift` 的 `list_models` 與 `list_backends` 改為以 family / size / runtime 三個獨立欄位回傳，並標示身分不完整的項目。**行為**：client 不需解析複合字串即可分組同一模型的多個 runtime。**驗證**：`StructuredModelListingTests` 主張 family / size / runtime 為三個獨立欄位、且不完整項目帶 `identity_complete: false`。（原本引用的「同一模型兩個 runtime 其 family 與 size 相同」與 `No entry spells the removed placeholder in any field` **兩則皆已移至 #187**——前者需要 parakeet size 正規化才成立。）（滿足 mcp-surface 的 "Model-listing tools report structured identity"）

## 8. 本 change 未關閉的項目（明列，不得當作已覆蓋）

> round 6 之後改採**讀時 canonical 化**（design D7），原本列在此處的四項——catalog re-key、從身分移除 `default`、`isComplete` 拒絕佔位字、兩筆 parakeet 為同一模型——**全部已落地**，因為它們的共同阻礙（「必須先遷移 383 筆記錄」）被證明不成立。剩下兩項：

- [x] 8.1 **`CommandCore` 的 seeding fallback 會捏造 family** —— **已消除，做法與原先設想的不同**。原本假設要「在解析失敗時猜出 family」，正解是**不要走到那一步**：`BenchmarkRunner` 自枚舉起就握有 `row.identity`，卻把它壓成字串，逼 persist 端從字串解析回身分。現在 `BenchmarkCandidate` 持有 `ModelID`（`model` 為導出屬性）、`BenchmarkRecord` 帶著它、persist 端直接用，**沒有解析、也就沒有解析失敗**。`BenchmarkStore` 的 legacy 遷移同理改為優先採用記錄自身的身分——它原本的 `family: "whisper", size: record.model` 在 `model` 變成 address 之後會產出 `size: "whisper/large-v3-turbo"`，`ModelID` 拒收含 `/` 的 size，那筆記錄的身分因而為 nil（由 3.3 的排除過濾器照出來）。whisper-only 的 fallback 保留給真正的 legacy 列。
- [ ] 8.2 **`mega-asr` 與 `qwen3-forcedaligner` 的 `size` 仍是佔位字**。兩者上游都沒有公布版本名，`ModelID` 又要求 size 非空。三個解各有代價：(i) 移除這兩列（`mlxFamilies` 從 15 掉到 13）；(ii) 另編一個 size 字串（換名字的佔位字）；(iii) 放寬 spec 容許 family-only 的 reference row。`ModelGridTests` 以**封閉列舉具名**這兩列。

## 7. 驗收

- [x] 7.1 全套測試綠燈：`/usr/bin/swift test` 回 **exit 0**、**564 tests / 115 suites**、0 issue（分支起點基線為 493 / 98）。並確認 `StoreProjection` 中不再存在任何依 backend 分支的邏輯（grep：該檔不含 `backendMLXAudio`）。

## 9. 收斂條件（round-8 verify 之後）

> round 8 的 devil's advocate 判定本 change **沒有在收斂**：round 4 是 catalog re-key 與 store key 分岔、round 6 是 address 規則與 backend 綁定、round 7 是 projection 與 consumer 分岔、round 8 是翻譯器與其安裝點分岔——「每一輪修掉的是實例，沒有一輪修掉產生實例的機制」。以下三項針對機制。

- [x] 9.1 **spy engine + 兩條路徑共用一則斷言**。`OptionsSpy` 記錄每次交給 engine 的 `TranscribeOptions`（`MockEngine.fixed` 原本把它丟棄，所以八輪以來沒有任何測試能觀察到任何路徑實際要求載入什麼）。`EngineSeam.expectCanonical` 是**唯一**的述詞，由 `transcribe`、`benchmark`、`--model` override 三處呼叫，另加一則 store-key 的端到端斷言。**變異驗證**（三次，各自 build + run）：(a) `transcribe` 退回 round-8 的「呼叫端先翻譯」→ 3 issues；(b) `WhisperKitEngine` 不翻譯 → 3 issues；(c) `BenchmarkRunner` 不帶 identity → store-key 斷言 3 issues。三者在修正後皆 exit 0。
- [x] 9.2 **`Fixtures.record` 經 `ModelGrid.canonical` 產生**。改收 `family:` / `size:` 兩個參數而非 `model: String`——**address 交不進來**，因此 production 不會產生的拼法在測試裡是寫不出來的。36 個呼叫端全部改寫；改寫後 14 則既有斷言失敗，全部是主張裸 size 的舊期望（正是 round 7、8 能全綠的原因）。
- [x] 9.4 **engine fixture 也走 canonical**（round-9 verify 的第四個變異）。條件 9.1 守的是「呼叫端交出什麼」，守不到「engine 拿它做什麼」——`EngineSeam` 驅動的是沒有翻譯步驟的 `MockEngine`，而三個 engine 的測試 fixture 餵的字串讓 `ModelGrid.engineName` **恰好是恆等函數**（`0.6b-v3` / `large-zh` / `small` 各命中一列原樣回傳；`qwen3-asr/4bit` 是 production 不產生的形狀、resolve 不到而 pass-through）。**實證**：刪掉 `ParakeetEngine` / `ChineseFamilyEngine` / `ExternalProcessEngine` 三處翻譯，564 tests 全綠、0 issue。**修法**：新增 `Fixtures.engineOptions(backend:family:size:)`——與 `Fixtures.record` 同機制，address 交不進來、由 `ModelGrid.canonical` 導出——並以 `FactorySpy` 斷言 factory 收到的是 runtime 自己的名字。修正後同一變異 4 issues。`ExternalProcessEngine` 未列入：它的測試 backend 是 `.mlxAudio`，該 runtime 的詞彙就是 address，翻譯在那裡可證明是恆等函數，不是未測路徑。
- [x] 9.5 **`canonical()` 不再捏造**。stored placeholder 遇到同一 identity 有多列且值不同時（whisper.cpp tiny 出 q5_1 與 q8_0），原本取**第一列**回傳 `.named("q5_1")` 並標記完整——不是保留佔位字，是為一筆從未這樣說的記錄**發明**一個具體值再為它背書。改為「目錄只在只有一個答案時作答」。**驗證**：`CanonicalFabricationTests` 兩則（多值 → `.unknown`；單值 → 照常解析）。
- [x] 9.6 **「身分是否完整」收斂成一份定義**。round 9 指出三處判準互異：MCP listing 要求 size 非佔位字 ∧ quantization 完整、`ModelGrid.comparable` 只看 quantization、`BenchmarkRecord.identityComplete` 只看 identity 非 nil——所以一列 size 是佔位字的模型會被 MCP 標 `identity_complete: false` 而同時進排序。現由 `ModelGrid.namesCompletely(identity:quantization:)` 單一定義，三處共用。
- [x] 9.3 **`ModelGrid.identity` 回三值**（`.resolved` / `.unknown` / `.ambiguous([ModelID])`）。編譯器逼出 8 個呼叫端逐一表態：`ExternalProcessEngine` 對 ambiguous 與 unknown **都不 pin**（理由不同，各自寫明）；`Router` 的 override 解析對 ambiguous **不再 fall through 到 `liveIdentity`**（那會答出第三個模型）並具名候選；`declaredSupport`（#105 安全閘）對 unknown **fail-open**、對 ambiguous **fail-closed**；`ModelRegistry.quantizations` 兩者皆回空。
