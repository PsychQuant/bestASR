"""bestASR — the intelligent model router for local speech recognition.

bestASR detects your machine (hardware, OS, acceleration backend, memory) and
your audio, then chooses the most suitable local ASR backend, model, and
compute type — and explains why.
"""

__version__ = "0.1.0"
